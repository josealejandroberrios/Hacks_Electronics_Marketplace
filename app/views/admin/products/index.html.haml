%button.btn.be-red.white= link_to('Agregar Producto', new_admin_product_path)
%br
%br
- if @products.empty?
  %h4 No Existen Productos en Stock!!!
- else
  %table.table.table-striped
    %thead
      %tr.sticky
        %th Nombre
        %th Imagen
        %th Descripcion
        %th Precio
        %th Stock
        %th Editar
        %th Eliminar
    %tbody
      - @products.each do |product|
        %tr
          %td= product.name
          %td 
            %img(src="#{product.image}")
          %td.font-size= product.description
          %td.currency.text-center= number_to_currency(product.price)
          %td.currency.text-center= product.quantity
          %td
            %buton.btn.be-red.white.but= link_to 'Editar', edit_admin_product_path(product)
          %td
            %button.btn.be-red.white.but= link_to 'Eliminar', admin_product_path(product), method: :delete, data: { confirm: "Are you sure?" }